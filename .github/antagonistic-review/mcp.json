{
  "mcpServers": {
    "verevoir": {
      "command": "npx",
      "args": ["-y", "github:verevoir/capabilities#v0.1.0"],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}
